
{% extends 'back/product/_base.html.twig' %}

{% block title %}Détail produit{% endblock %}

{% block body %}
<<<<<<< HEAD

 <a class = "btn btn-success" href="{{ path('app_back_product_list') }}">Retour à la liste</a>

<div class="container">
    <h1>{{ product.name }}</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ product.name }}</td>
            </tr>
            <tr>
                <th>category</th>
                <td>{{ product.id }}</td>
            </tr>
            <tr>
                <th>Stock</th>
                <td>{{ product.stock }}</td>
            </tr>
            <tr>
                <th>Unity</th>
                <td>{{ product.unity }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ product.description }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_back_product_edit', {'id': product.id}) }}">Modifier</a>

    {# {{ include('back/product/_delete.html.twig') }} #}
=======
<section class="container-fluid">
    <h1 class="text-center">Détail du produit : {{ product.name }}</h1>
    <a class="btn btn-outline-warning btn-sm" href="{{ path('app_back_product_edit', {'id': product.id}) }}"><i class="bi bi-pencil-fill"> Modifier</i></a>
    
    <div class="row justify-content-around mt-2">
        <article class="card col-4">
            <img class="card-img-top" src="{{ baseUrl ~ asset('images/products/'~ product.image)  }}" alt="Photo de {{ product.name }}">
            <div class="card-body">
                <ul>
                    <li class="kist-group-item"><strong class="fw semibold">Nom : </strong>{{ product.name }}</li>
                    <li class="kist-group-item"><strong class="fw semibold">Catégorie : </strong>{{ product.category.name }}</li>
                    <li class="kist-group-item"><strong class="fw semibold">Stock en vente : </strong>{{ product.stock }}</li>
                    <li class="kist-group-item"><strong class="fw semibold">Unité : </strong>{{ product.unity }}</li>
                    <li class="kist-group-item"><strong class="fw semibold">Prix (à l'unité) : </strong>{{ product.price }}</li>
                </ul>
            </div>
        </article>
        <article class="card col-6">
            <h3 class="card-title">Description du produit</h3>
            <p class="card-text">
                {{ product.description }}
            </p>
        </article>
    </div>
>>>>>>> f577d3355f41a76ce78a63fcf58b512597f8b4cb
</div>
</section>
{% endblock %}

