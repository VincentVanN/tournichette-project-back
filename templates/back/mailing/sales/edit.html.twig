{% extends 'base-back.html.twig' %}

{% block title %}Mail des ventes{% endblock %}

{% block body %}
<div class="container">

    <h1 class="text-center">Envoi des mails de ventes</h1>

    <p>
        Sur cette page, vous pouvez rédiger les mails qui seront automatiquement envoyés lors de l'ouverture ou la fermeture
        des ventes.<br>
        Vous pouvez aussi activer ou désactiver cette fonctionnalité.
    </p>

    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <i class="bi bi-check-circle-fill"></i> {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
    {% endfor %}    

    {{ form_start(form, {"attr" : {"novalidate": "novalidate"}}) }}
        {{ form_errors(form) }}
        {{ form_row(form.sendMail) }}
        <div class="accordion" id="accordionExample">
            <div class="accordion-item">
                <h2 class="accordion-header" id="headingOne">
                <button class="accordion-button" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
                    Mail d'ouverture des ventes
                </button>
                </h2>
                <div id="collapseOne" class="accordion-collapse collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
                <div class="accordion-body">
                    <div class="input-group mb-3">
                        <span class="input-group-text" id="basic-addon1">Sujet : </span>
                        {{ form_widget(form.startMailSubject, {"attr" : {"aria-describedby": "basic-addon1"}}) }}
                    </div>

                    {{ form_widget(form.startMail) }}
                </div>
                </div>
            </div>
            <div class="accordion-item">
                <h2 class="accordion-header" id="headingTwo">
                <button class="accordion-button collapsed" type="button" data-bs-toggle="collapse" data-bs-target="#collapseTwo" aria-expanded="false" aria-controls="collapseTwo">
                    Mail de fermeture des ventes
                </button>
                </h2>
                <div id="collapseTwo" class="accordion-collapse collapse" aria-labelledby="headingTwo" data-bs-parent="#accordionExample">
                <div class="accordion-body">
                    <div class="input-group mb-3">
                        <span class="input-group-text" id="basic-addon2">Sujet : </span>
                        {{ form_widget(form.endMailSubject, {"attr" : {"aria-describedby": "basic-addon2"}}) }}
                    </div>
                    {{ form_widget(form.endMail) }}
                </div>
                </div>
            </div>
        </div>
        <div class="justify-content-center d-flex mt-3">
            <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        </div>
    
    {{ form_end(form) }}

</div>
{% endblock %}