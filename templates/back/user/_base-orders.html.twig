{% extends 'base-back.html.twig' %}


{% block subnav %}
<nav class="navbar navbar-expand-lg navbar-dark bg-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item m-1">
                    <a class="nav-link text-white btn btn-success" href="{{ path('app_back_order_list') }}">Liste des commandes</a>
                </li>
                <li class="nav-item m-1">
                    <a class="nav-link text-white btn btn-success" href="{{ path('app_back_user_orders', {'id': user.id}) }}">Liste des commandes de {{ user.firstname }} {{ user.lastname }}</a>
                </li>
                <li class="nav-item m-1">
                    <a class="nav-link text-white btn btn-success" href="{{ path('app_back_user_list') }}">Liste des utilisateurs</a>
                </li>
                {# <li class="nav-item m-1">
                    <a class="nav-link text-white btn btn-success" href="{{ path('app_back_user_new') }}">Ajouter un utilisateur</a>
                </li> #}
            </ul>
        </div>
    </div>
</nav>
{% endblock %}