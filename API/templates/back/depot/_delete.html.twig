<form method="post" action="{{ path('app_back_depot_delete', {'id': depot.id}) }}" onsubmit="return confirm('Etes vous sur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depot.id) }}">
    <button class="btn btn-danger mt-1">Supprimer</button>
</form>