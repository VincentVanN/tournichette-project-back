{% extends 'back/depot/_base.html.twig' %}

{% block title %depot{% endblock %}

{% block body %}

<div class="container">
    <h1>depot</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depot.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ depot.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_back_depot_edit', {'id': depot.id}) }}">Modifier</a>

    {{ include('back/depot/_delete.html.twig') }}
</div>
{% endblock %}