{% extends 'back/category/_base.html.twig' %}

{% block title %}category index{% endblock %}

{% block body %}
    <h1>category index</h1>

    <table class="table table-condensed">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
            </tr>
        </thead>
        <tbody>
        {% for category in categorys %}
            <tr>
               <td>{{ category.id }}</td>
                <td>{{ category.name }}</td>
                <td>
                    <a href="{{ path('app_back_category_show', {'id': category.id}) }}" class = "btn btn-info">show</a>
                    <a href="{{ path('app_back_category_edit', {'id': category.id}) }}" class = "btn btn-warning">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_back_category_new') }}"class = "btn btn-success">Create new</a>

{% endblock %}
