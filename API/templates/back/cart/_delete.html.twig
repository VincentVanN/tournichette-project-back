<form method="post" action="{{ path('app_back_cart_delete', {'id': cart.id}) }}" onsubmit="return confirm('Etes vous sur ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cart.id) }}">
    <button class="btn btn-danger mt-1">Supprimer</button>
</form>