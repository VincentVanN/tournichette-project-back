{% extends 'back/order/_base.html.twig' %}

{% block title %Order{% endblock %}

{% block body %}

<div class="container">
    <h1>Order</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ order.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ order.name }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('app_back_order_edit', {'id': order.id}) }}">Modifier</a>

    {{ include('back/order/_delete.html.twig') }}
</div>
{% endblock %}